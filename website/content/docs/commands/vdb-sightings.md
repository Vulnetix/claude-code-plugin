---
title: vdb-sightings
weight: 50
description: Merged in-the-wild observation timeline for a CVE
---

Merged in-the-wild observation timeline for a CVE

## Invocation

```
/vulnetix:vdb-sightings [arguments]
```

Runs:

```bash
vulnetix vdb sightings $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
