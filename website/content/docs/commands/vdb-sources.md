---
title: vdb-sources
weight: 50
description: List vulnerability data sources
---

List vulnerability data sources

## Invocation

```
/vulnetix:vdb-sources [arguments]
```

Runs:

```bash
vulnetix vdb sources $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
