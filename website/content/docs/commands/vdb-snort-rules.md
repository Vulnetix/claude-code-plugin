---
title: vdb-snort-rules
weight: 50
description: Look up Snort detection rules with rich filters
---

Look up Snort detection rules with rich filters

## Invocation

```
/vulnetix:vdb-snort-rules [arguments]
```

Runs:

```bash
vulnetix vdb snort-rules $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
