---
title: vdb-purl
weight: 50
description: Query VDB using a Package URL (PURL)
---

Query VDB using a Package URL (PURL)

## Invocation

```
/vulnetix:vdb-purl [arguments]
```

Runs:

```bash
vulnetix vdb purl $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
