---
title: vdb-product
weight: 50
description: Get product version information
---

Get product version information

## Invocation

```
/vulnetix:vdb-product [arguments]
```

Runs:

```bash
vulnetix vdb product $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
