---
title: vdb-status
weight: 50
description: Check API health and display CLI metadata
---

Check API health and display CLI metadata

## Invocation

```
/vulnetix:vdb-status [arguments]
```

Runs:

```bash
vulnetix vdb status $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
