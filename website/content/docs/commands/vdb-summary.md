---
title: vdb-summary
weight: 50
description: Get global VDB database statistics
---

Get global VDB database statistics

## Invocation

```
/vulnetix:vdb-summary [arguments]
```

Runs:

```bash
vulnetix vdb summary $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
