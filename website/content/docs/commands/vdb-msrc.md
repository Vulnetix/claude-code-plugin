---
title: vdb-msrc
weight: 50
description: Microsoft Security Response Center patch-Tuesday rollups
---

Microsoft Security Response Center patch-Tuesday rollups

## Invocation

```
/vulnetix:vdb-msrc [arguments]
```

Runs:

```bash
vulnetix vdb msrc $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
