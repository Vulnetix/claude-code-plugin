---
title: vdb-metrics
weight: 50
description: Vulnerability metric intelligence
---

Vulnerability metric intelligence

## Invocation

```
/vulnetix:vdb-metrics [arguments]
```

Runs:

```bash
vulnetix vdb metrics $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
