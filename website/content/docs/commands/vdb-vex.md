---
title: vdb-vex
weight: 50
description: Retrieve VEX statements
---

Retrieve VEX statements

## Invocation

```
/vulnetix:vdb-vex [arguments]
```

Runs:

```bash
vulnetix vdb vex $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
