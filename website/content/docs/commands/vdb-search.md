---
title: vdb-search
weight: 50
description: Search CVE identifiers by prefix
---

Search CVE identifiers by prefix

## Invocation

```
/vulnetix:vdb-search [arguments]
```

Runs:

```bash
vulnetix vdb search $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
