---
title: vdb-ids
weight: 50
description: List CVE identifiers published in a calendar month
---

List CVE identifiers published in a calendar month

## Invocation

```
/vulnetix:vdb-ids [arguments]
```

Runs:

```bash
vulnetix vdb ids $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
