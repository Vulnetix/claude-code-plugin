---
title: env
weight: 50
description: Display the current Vulnetix environment context
---

Display the current Vulnetix environment context

## Invocation

```
/vulnetix:env [arguments]
```

Runs:

```bash
vulnetix env $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
