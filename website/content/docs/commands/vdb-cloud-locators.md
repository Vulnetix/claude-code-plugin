---
title: vdb-cloud-locators
weight: 50
description: Get cloud resource locator templates for a vendor/product (V2)
---

Get cloud resource locator templates for a vendor/product (V2)

## Invocation

```
/vulnetix:vdb-cloud-locators [arguments]
```

Runs:

```bash
vulnetix vdb cloud-locators $ARGUMENTS -V v2 -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
