---
title: vdb-cwe
weight: 50
description: Look up CWE-related vulnerability intelligence (V2)
---

Look up CWE-related vulnerability intelligence (V2)

## Invocation

```
/vulnetix:vdb-cwe [arguments]
```

Runs:

```bash
vulnetix vdb cwe $ARGUMENTS -V v2 -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
