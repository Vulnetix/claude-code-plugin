---
title: gha-upload
weight: 50
description: GitHub Actions artifact batch upload
---

GitHub Actions artifact batch upload

## Invocation

```
/vulnetix:gha-upload [arguments]
```

Runs:

```bash
vulnetix gha upload $ARGUMENTS
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
