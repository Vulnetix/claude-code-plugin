---
title: gha-status
weight: 50
description: GitHub Actions artifact status polling
---

GitHub Actions artifact status polling

## Invocation

```
/vulnetix:gha-status [arguments]
```

Runs:

```bash
vulnetix gha status $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
