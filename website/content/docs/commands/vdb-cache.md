---
title: vdb-cache
weight: 50
description: Manage the local VDB response cache
---

Manage the local VDB response cache

## Invocation

```
/vulnetix:vdb-cache [arguments]
```

Runs:

```bash
vulnetix vdb cache $ARGUMENTS
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
