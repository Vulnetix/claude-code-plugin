---
title: vdb-vendor-trends
weight: 50
description: Vendor trend data — monthly/yearly CVE+GHSA breakdown
---

Vendor trend data — monthly/yearly CVE+GHSA breakdown

## Invocation

```
/vulnetix:vdb-vendor-trends [arguments]
```

Runs:

```bash
vulnetix vdb vendor-trends $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
