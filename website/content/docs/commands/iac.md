---
title: iac
weight: 50
description: Analyze Terraform / OpenTofu / Nix infrastructure-as-code
---

Analyze Terraform / OpenTofu / Nix infrastructure-as-code

## Invocation

```
/vulnetix:iac [arguments]
```

Runs:

```bash
vulnetix iac $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
