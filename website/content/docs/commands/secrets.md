---
title: secrets
weight: 50
description: Run Vulnetix secret detection only
---

Run Vulnetix secret detection only

## Invocation

```
/vulnetix:secrets [arguments]
```

Runs:

```bash
vulnetix secrets $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
