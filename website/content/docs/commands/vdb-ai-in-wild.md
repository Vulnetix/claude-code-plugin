---
title: vdb-ai-in-wild
weight: 50
description: AI-discovered in-the-wild exploitation observations
---

AI-discovered in-the-wild exploitation observations

## Invocation

```
/vulnetix:vdb-ai-in-wild [arguments]
```

Runs:

```bash
vulnetix vdb ai-in-wild $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
