---
title: vdb-yara-rules
weight: 50
description: Look up YARA static-analysis rules with rich filters
---

Look up YARA static-analysis rules with rich filters

## Invocation

```
/vulnetix:vdb-yara-rules [arguments]
```

Runs:

```bash
vulnetix vdb yara-rules $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
