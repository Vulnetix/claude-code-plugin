---
title: containers
weight: 50
description: Analyze Dockerfile / Containerfile / compose
---

Analyze Dockerfile / Containerfile / compose

## Invocation

```
/vulnetix:containers [arguments]
```

Runs:

```bash
vulnetix containers $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
