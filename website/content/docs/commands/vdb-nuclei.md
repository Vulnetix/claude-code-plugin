---
title: vdb-nuclei
weight: 50
description: Get Nuclei templates referencing a CVE
---

Get Nuclei templates referencing a CVE

## Invocation

```
/vulnetix:vdb-nuclei [arguments]
```

Runs:

```bash
vulnetix vdb nuclei $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
