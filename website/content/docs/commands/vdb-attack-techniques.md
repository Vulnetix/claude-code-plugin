---
title: vdb-attack-techniques
weight: 50
description: Look up MITRE ATT&CK technique mappings for CVEs
---

Look up MITRE ATT&CK technique mappings for CVEs

## Invocation

```
/vulnetix:vdb-attack-techniques [arguments]
```

Runs:

```bash
vulnetix vdb attack-techniques $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
