---
title: vdb-scorecard
weight: 50
description: Get vulnerability scorecard (V2)
---

Get vulnerability scorecard (V2)

## Invocation

```
/vulnetix:vdb-scorecard [arguments]
```

Runs:

```bash
vulnetix vdb scorecard $ARGUMENTS -V v2 -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
