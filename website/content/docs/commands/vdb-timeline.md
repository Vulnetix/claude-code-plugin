---
title: vdb-timeline
weight: 50
description: Get vulnerability lifecycle timeline
---

Get vulnerability lifecycle timeline

## Invocation

```
/vulnetix:vdb-timeline [arguments]
```

Runs:

```bash
vulnetix vdb timeline $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
