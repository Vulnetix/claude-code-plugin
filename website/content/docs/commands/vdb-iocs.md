---
title: vdb-iocs
weight: 50
description: IOC pivots from CrowdSec sightings + Shadowserver counts
---

IOC pivots from CrowdSec sightings + Shadowserver counts

## Invocation

```
/vulnetix:vdb-iocs [arguments]
```

Runs:

```bash
vulnetix vdb iocs $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
