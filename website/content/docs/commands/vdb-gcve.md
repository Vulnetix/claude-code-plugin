---
title: vdb-gcve
weight: 50
description: Get CVEs by date range
---

Get CVEs by date range

## Invocation

```
/vulnetix:vdb-gcve [arguments]
```

Runs:

```bash
vulnetix vdb gcve $ARGUMENTS -o json
```

The command is a deterministic CLI wrapper (`disable-model-invocation: true`). Output is JSON; the model summarizes it for the user.

## See also

- [Commands index](/docs/commands/)
