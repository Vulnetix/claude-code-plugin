{
  "skill_name": "capabilities-detect",
  "should_trigger": [
    "What security tools do I have installed right now?",
    "Probe my environment for Pix",
    "I just installed semgrep — refresh the capability state",
    "I added a Dockerfile to the repo; re-detect the repo signals",
    "Show me what Pix currently believes about my environment",
    "Force-refresh `.vulnetix/capabilities.yaml`",
    "Another skill is reporting unknown capability — fix that",
    "Re-run the capabilities probe, mine looks stale",
    "Detect which binaries Pix can use",
    "I added Terraform files — update the IaC signal"
  ],
  "should_not_trigger": [
    "Run a SAST scan on the diff (should route to sast-scan)",
    "Look up CVE-2024-3094 (should route to vuln)",
    "Show me what's tracked in memory.yaml (should route to dashboard)",
    "Scan my Dockerfile for misconfigs (should route to container-scan)",
    "Find exploited CVEs in npm this week (should route to exploits-search)",
    "Generate an SBOM (should route to sbom-generate)",
    "Check if my deps have known CVEs (should route to vuln)",
    "Audit my package licenses (should route to license-check)",
    "Is my Node version EOL? (should route to eol-check)",
    "Apply a fix for CVE-2021-44228 (should route to fix)"
  ]
}
